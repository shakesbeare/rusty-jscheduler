[package]
name = "scheduler"
version = "0.1.0"
license = "ISC"
edition = "2018"
exclude = ["index.node"]

[lib]
crate-type = ["cdylib"]

[dependencies]
chrono = "0.4"

[dependencies.neon]
version = "0.10"
default-features = false
features = ["napi-6"]
